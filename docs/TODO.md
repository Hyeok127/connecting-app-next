# 해야 할 일 — 통합 백로그

작성 2026-09-02. 기준 `origin/dev` (`b3524f7`).
출처: ① 전수조사 111건(`AUDIT_2026-09-02.md`) ② `STATUS.md`의 미처리 목록 ③ 이번 세션 신규 발견.

> **감사 111건은 `main` 기준이었다.** `dev`에서 이미 해결된 것이 다수라, 아래 목록은
> **`dev` HEAD에 실제로 살아있는지 코드로 대조한 것만** 담았다. 대조하지 않은 항목은 `미확인`으로 표시했다.

## 진행 상황 (2026-09-02 세션)

**완료** — `dev`에 커밋·푸시됨. 검증: `npm test` 19/19 · `tsc --noEmit` · `lint` · `build` · `smoke_run.sh`

| 항목 | 커밋 |
|---|---|
| 확정 결함 3건(만남 자동종료 int4 오버플로·연락처 입력 언마운트·순위 가드) | `b67a15b` |
| P2-1~P2-7 매칭 하드필터 fail-open·null 비대칭·나이 경계·가입순 고정 노출·죽은 함수 | `278f752` |
| P1-2 차단 시 정보 공개 중단 + 사진 동의 철회 / P1-3 초대 상한 / `select("1")` 잔여 2건 | `f455be8` |
| P2-12·P2-13 중요도 가중치 + 절대조건 3개 제한 + **첫 자동화 테스트** | `7cbb5a1` |
| P2-14 관계 지향(`intent`) 축 + 멀티모드 최소 규칙 | `3c4ec61` |

**막힌 것 — 사용자 조치가 필요**

| 항목 | 무엇이 필요한가 |
|---|---|
| P0-1 `dev` → `main` 머지 | **프로덕션 배포 승인.** 코드는 준비됨 |
| P0-2 `011` 마이그레이션 적용 | Supabase SQL Editor 실행 (이번 세션에서 건너뛰기로 결정) |
| P0-3 Preview DB 확인 | `VERCEL_TOKEN`이 있는 기기(nt9)에서 `node scripts/check_preview_env.mjs` |
| P1-1 연락처 매칭별 스냅샷 | `matches`에 컬럼 추가 필요 → DDL |
| P3-1 DB 백업 · P3-2 Sentry | 외부 계정·자격증명 |

**남은 설문 확장** — P2-15~P2-20(`birth_year`·`intro`·성격 5축·데이트 스타일·트레이드오프·점수식 3층화).
`birth_year`·`intro`는 DDL이 필요하고, 성격 5축·데이트 스타일은 `intent`와 같은 방식으로
`life_values` jsonb에 얹으면 DDL 없이 가능하다.

---

**현황 요약**

| 지표 | 값 |
|---|---|
| 운영 코드(`origin/main`) | `e177b74` (2026-08-14) — `origin/dev`보다 **8커밋 뒤**(전부 2026-09-02 작업분) |
| 운영 DB | 마이그레이션 **010까지 적용됨** — 운영 코드와 일치 |
| 운영 회원 | 3명 / 개발 DB 220명 |
| 미적용 마이그레이션 | `006_cleanup`(의도적 보류), `011_fix_autoclose_overflow`(신규) |

---

## P0 — 즉시 (운영이 실제로 깨져 있음)

| # | 할 일 | 근거 | 상태 |
|---|---|---|---|
| P0-1 | **`dev` → `main` 머지·배포** | 이번 세션 수정 8커밋이 미배포. 연락처 입력 언마운트·순위 가드·매칭 fail-open 등이 아직 운영에 없다. (※ 초판에 "44커밋 뒤"라고 적었던 것은 **로컬 `main` 참조가 낡아서** 생긴 오류다 — 아래 P5-6 참고) | 미착수 |
| P0-2 | **`011_fix_autoclose_overflow.sql` 적용** | `30 * 86400000` int4 오버플로로 30일 만남 자동종료가 **한 번도 동작한 적 없음**. 사용자가 `dating`에 갇혀 배치에서 영구 제외 | 코드 커밋됨 · **DB 미적용** |
| P0-3 | **Vercel Preview가 어느 DB를 보는지 확인** | Preview 환경변수가 Production과 같으면 **모든 dev 테스트가 실사용자 데이터를 건드린다**. 220명 개발 DB와 3명 운영 DB가 섞였는지 즉시 확인 | 미확인 |
| P0-4 | **Supabase 액세스 토큰 교체** | 채팅에 노출 이력. **모든 Supabase 프로젝트를 관리(DB 삭제 포함)** 가능한 전권 토큰 | 사용자 보류 결정 |
| P0-5 | 밤 8시 배치 첫 실행 확인 | 크론이 그동안 한 번도 실행되지 않았음(`1eebcc1`에서 수정). 수정본이 실제로 도는지 미검증 | P0-1 이후 |

---

## P1 — 개인정보·보안 (실사용자 늘기 전에)

| # | 할 일 | 근거 | 상태 |
|---|---|---|---|
| P1-1 | **연락처를 매칭별로 스냅샷** | `users.contact` 단일 필드를 모든 매칭이 공유. 새 매칭에서 번호를 바꾸면 **예전 상대에게도 새 번호가 보인다** | STATUS.md |
| P1-2 | **차단 시 이미 공개된 정보 숨기기** | 차단은 추천에서만 제외. 공개된 연락처·교환한 사진은 계속 보임. **사진 동의 철회 수단 없음** | STATUS.md |
| P1-3 | **초대 코드 상한·만료** | 만료 없음, 인당 상한 없음. 한 사람이 다수 계정으로 추천 풀을 채울 수 있고 초대자 포인트도 누적 | STATUS.md |
| P1-4 | 세션을 httpOnly 쿠키로 | 토큰이 `localStorage`(XSS로 읽힘) + TTL 30일. CSP 없음 | STATUS.md · 감사 |
| P1-5 | 탈퇴에 PIN 재확인·속도 제한 | 토큰만으로 계정 영구 삭제 가능 | STATUS.md |
| P1-6 | 고아 사진 정리 크론 | 업로드했지만 프로필에 연결되지 않은 객체가 R2에 영구 잔존 | STATUS.md |
| P1-7 | 사진 objectPath 소유권 검증 | 남의 사진 경로를 자기 프로필로 지정하거나 삭제 유발 가능 | 감사 · **dev 미확인** |
| P1-8 | `PUT /api/me`의 `name` 검증 | 로그인 식별자인 이름을 검증 없이 변경 — 빈 이름·이름 선점 | 감사 · **dev 미확인** |
| P1-9 | 업로드 MIME·매직바이트·용량 검증 | 확장자만 검증. 인증은 `1eebcc1`에서 필수화됨 | 감사 · 부분 해결 |

---

## P2 — 매칭 품질 (설문 v2 · 상세는 `SURVEY_V2.md`)

### P2-A. 지금 코드에 살아있는 매칭 결함 (dev 대조 완료)

| # | 할 일 | 위치 | 내용 |
|---|---|---|---|
| P2-1 | **선호 미설정 시 하드필터 전면 fail-open** | `lib/matching.ts:150` | `preferences` 행이 없으면 `myPrefs=null` → `fits(null)`이 무조건 `true`. **성별 필터도 없이 전 회원이 추천에 섞인다** |
| P2-2 | **`pref_genders`를 가입 필수로 승격** | `signup/route.ts:130` | 현재 선택 항목. 미설정 시 P2-1과 결합해 **추천 절반이 동성** |
| P2-3 | **`fits()`의 null 처리 비대칭** | `lib/matching.ts:82~86` | `jobTypes`/`jobRoles`는 `?? ""`로 **빈값이면 탈락**, `mbti`는 `u.mbti &&`로 **빈값이면 통과**. 빈칸일수록 유리해지는 역인센티브 |
| P2-4 | **나이 경계 truthy 버그** | `lib/matching.ts:80~81` | `if (prefs.age_min && ...)` — `0`이 "필터 없음"이 되고 `age_min > age_max` 검증도 없어 공집합 설정이 저장된다 |
| P2-5 | **MBTI를 하드필터에서 제거** | `lib/matching.ts:86` | 16칸 완전일치를 상호로 곱하면 소규모 폐쇄망에서 후보풀이 실질 0. 표시 전용으로 강등 |
| P2-6 | 죽은 함수 2개 제거 또는 수정 | `lib/matching.ts:90,100` | `hasMatchHistory`·`isAutoExcluded`가 **어디서도 호출되지 않는다**(N+1 제거 때 인라인화). `maybeSingle()` fail-open 버그를 안은 채 방치 |
| P2-7 | 동점 tie-break 결정론적 셔플 | `lib/matching.ts:190` | `sim → trust_score → created_at`. 키워드 없는 회원은 `sim=0`, `trust_score`는 전원 50 → **가입순 고정 노출** |
| P2-8 | `.or()` 문자열 보간 이스케이프 | `lib/matching.ts:94,141,148` | 현재 id는 내부 생성이라 주입 불가하나 이스케이프 계층이 없다 |
| P2-9 | 배치가 `preferences`를 재검증하지 않음 | `003_batch.sql` | 순위 확정 후 바뀐 선호가 무시된 채 매칭 생성 | 
| P2-10 | 매칭 실패자 처리·통지 없음 | `003_batch.sql` | 아무와도 매칭되지 않은 사람에게 아무 일도 일어나지 않는다 |
| P2-11 | 성비 불균형 대응 없음 | `003_batch.sql` | 탐욕 배정 + 매칭 이력 영구 제외 + 3회 미매칭 제외로 후보 풀이 **단조 감소만** 한다 |

### P2-B. 설문 확장 (`SURVEY_V2.md` G1~G8)

| # | 할 일 | 효과/비용 |
|---|---|---|
| P2-12 | **G1 중요도 가중치** — `value_prefs`를 `{accepted, importance}` 객체로 승격 + 구형 배열 하위호환 파서 | **최우선.** 현재 "반드시 무교"와 "문신 없으면 좋겠다"가 똑같이 1점 |
| P2-13 | **G2 딜브레이커** — `importance=3`만 하드필터로, 최대 3개 DB 트리거 제한 | 높음 |
| P2-14 | **G3 `intent`(진지/자연스럽게/가볍게)** + 필터 규칙 | **효과 대비 비용 최고.** 컬럼 1개 + 필터 1줄로 멀티모드 성립 |
| P2-15 | G6 `birth_year` — 나이 정수 스냅샷 폐지 | 중 |
| P2-16 | G7 `intro` 자기소개 — 닉네임제·사진 비공개라 카드에 남는 정보가 거의 없음 | 중 |
| P2-17 | G4 성격 5축 척도 | 중 |
| P2-18 | G5 연락·데이트 스타일 4문항 | 중 |
| P2-19 | G8 트레이드오프 7문항 + 우선순위 TOP3 | 가중치 데이터 소스 |
| P2-20 | 2·3층 점수식(가중 유사도 + 상호성 조화평균) 교체 — **shadow 실행 필수** | 높음·위험 |

---

## P3 — 운영·관측

| # | 할 일 | 근거 |
|---|---|---|
| P3-1 | **DB 백업** | Supabase 무료엔 자동 백업 없음. PITR 설정 여부 미확인, 덤프 스크립트 없음. **가장 시급한 인프라 항목** |
| P3-2 | **에러 모니터링(Sentry) + 로그 드레인** | DB 쓰기 실패를 대부분 확인하지 않아(`{ error }` 무시) **절반만 써져도 `200 OK`가 나간다** |
| P3-3 | `/api/health`에 업타임 모니터 연결 | 만들어만 두고 감시 주체가 없음 |
| P3-4 | 배치 실패 알림 | 실패가 조용히 지나감 |
| P3-5 | pg_cron 실패 관측 | 명령이 스키마 미지정이고 실패가 어디에도 드러나지 않음 (P0-2가 그 실증) |
| P3-6 | 스케일 한계 해소 | `recommendations`가 활성 회원 전체를 `select *`(추천 조회 + 순위 검증 2회). 관리자 화면은 전체 회원 + 전체 `point_events` 무제한 조회. 수백 명까진 무해, 수천 명이면 문제 |
| P3-7 | `006_cleanup.sql` 적용 | 005 안정화 확인 후. 폴백 코드 제거 → 적용 순서 STATUS.md 참고 |
| P3-8 | 운영 키를 개발 기기에서 분리 | `.env.local.prod.bak`에 prod service_role. 주석 토글 방식이라 실수 시 운영 데이터 직접 접근 |
| P3-9 | 자동화 테스트 도입 | `smoke_*.mjs` 6종이 있으나 CI 없음. 회귀 방어력 부재 |
| P3-10 | nt9 → 타 노드 SSH 복구 | `Permission denied (publickey)` |
| P3-11 | keepalive를 로그아웃 상태에서도 동작하게 | 관리자 권한 + "로그온 여부와 무관하게 실행"로 재설정 필요 |

---

## P4 — 코드 위생·UX

| # | 할 일 | 위치 |
|---|---|---|
| P4-1 | **상수 드리프트 해소** | `TOP_N`·`RESPOND_HOURS`·`RANKING_VALID_DAYS`·`MEETING_MAX_DAYS`가 **코드 어디서도 안 쓰인다**(전부 SQL 하드코딩). 값을 바꿔도 동작이 안 바뀌는 함정 |
| P4-2 | Toast에 `aria-live` 추가 | `components/Toast.tsx` — 유일한 피드백 채널인데 스크린리더에 안 읽히고 2.5초 후 사라짐 |
| P4-3 | 라벨-입력 연결 | 상당수 입력이 `placeholder`만으로 설명됨 |
| P4-4 | 아이콘 버튼 접근명 | 순위 편집 ↑↓✕에 접근 가능한 이름 없음 |
| P4-5 | 반응형 점검 | 사실상 모바일 전용인데 고정 2·3열 폼 존재 |
| P4-6 | 색 대비 | `text-slate-400` 계열이 흰 배경에서 WCAG AA 미달 |
| P4-7 | API 실패가 빈 상태로 위장 | 에러를 삼키고 "추천이 없어요"로 표시 |
| P4-8 | 순위 확정에 확인 단계 | 되돌릴 수 없는 동작. 중복 클릭 방지는 `confirmedToday`로 되어 있으나 사전 확인 없음 |
| P4-9 | 사진 업로드 진행률·부분 실패 처리 | 없음. HEIC가 jpg로 위장되는 문제 포함 |
| P4-10 | 한국어 존댓말 체계 통일 | 해요체/합니다체 혼용, 역할 용어 흔들림 |
| P4-11 | `paused` 탈출 수단 | 교제 후 헤어져도 복귀 방법 없음(관리자 unpause 부재) |
| P4-12 | 주선자(bridge) 화면 | `/bridge`와 `/profile`이 완전히 같은 페이지. 편집 가능한 화면 없음 |
| P4-13 | lint 경고 3건 정리 | 미사용 변수 2건 + 커스텀 폰트 경고 1건 |

---

## P5 — 법적·문서

| # | 할 일 |
|---|---|
| P5-1 | 약관·개인정보처리방침 **법률 검토** (현재 초안, 미검토) |
| P5-2 | 개인정보 보호책임자·연락처 명시 |
| P5-3 | `STATUS.md` 갱신 — 기준 커밋이 `039b86c`(2026-08-12)로 30커밋 이상 낡음 |
| P5-4 | 이 저장소 체크아웃 브랜치 정리 — 작업 위해 `main` → `dev`로 옮겨둠 |
| P5-5 | 감사 미완 축 재실행 — **API 라우트 19개 전수 점검**, **운영·성능** 2개 축 + **검증 단계 전체**가 세션 한도로 미실행 |

---

## P6 — 머지·배포 체계 (2026-09-02 점검)

브랜치 전략(`dev`=Preview / `main`=Production) 자체는 **설계대로 동작한다.**
오늘 dev push 6건이 전부 Preview 배포만 만들었고 Production은 무변동임을 배포 이력으로 확인했다.
문제는 브랜치가 아니라 **그 주변의 게이트와 추적**에 있다.

| # | 문제 | 증거 | 처방 |
|---|---|---|---|
| **P6-1** ✅ | **마이그레이션 적용 상태를 아무 데서도 추적하지 않는다** | `schema_migrations` 류 테이블 없음. `apply_migration.mjs`는 `console.log`만 하고 기록을 남기지 않음. 적용 여부가 **`STATUS.md` 산문**("dev/prod 적용 완료")에만 존재 | **완료** `531515b` — `012_schema_migrations.sql` + `apply_migration.mjs` 기록 + `/api/health`의 `migrations.pending`/`orphan`. ⚠️ **012를 DB에 적용해야 추적이 시작된다** |
| **P6-2** ✅ | **CI가 없다** | `.github/workflows/` 부재. `npm test`·`lint`·`build`가 어디서도 자동 실행되지 않음 | **완료** `c39e8df` — 첫 실행 `verify` 성공. push는 `~/.git-credentials`의 PAT(`repo, workflow`)를 `store` 헬퍼로 강제해 해결(방법은 CLAUDE.md) |
| **P6-3** ✅ | **`main` 브랜치 보호가 없다** | `gh api .../branches/main/protection` → 404 "Branch not protected". PR 이력 **0건**, 전부 직접 push | **완료** — force push·삭제 차단 + **필수 체크 `verify`**. PR 필수는 끔(1인 개발). FF 릴리스는 SHA가 같아 dev에서 통과한 체크가 그대로 인정됨을 실측 확인 |
| **P6-4** ✅ | **로컬 `main`이 썩는다 — 판단을 6배 틀리게 만든다** | 이 체크아웃의 로컬 `main`=`e47c301`(8/09), `origin/main`=`e177b74`(8/14). `git log main..dev`가 **51**, `origin/main..dev`는 **8**. 이 세션에서 실제로 오판했다 | **완료** — CLAUDE.md "브랜치 비교는 반드시 `origin/main` 기준으로" 절 추가 |
| **P6-5** ✅ | **롤백 절차가 문서에 없다** | `main` push = 즉시 프로덕션인데 되돌리는 방법이 CLAUDE.md·README 어디에도 없음 | **완료** — CLAUDE.md "롤백" 절 추가. Vercel Instant Rollback → git revert 순. **DB는 롤백되지 않는다**는 점 명시 |
| **P6-6** ✅ | 미적용 마이그레이션이 파일만 봐선 구분되지 않는다 | `006_cleanup`은 의도적 보류, `011`은 신규 미적용 — 둘 다 파일에 표시가 없어 `STATUS.md`를 읽어야 안다 | **완료** — P6-1이 해소. `/api/health`가 실제 DB 상태로 답한다 |
| **P6-8** ✅ | `README`가 없는 `.env.example`을 복사하라고 안내 | `cp .env.example .env.local`(README:31)인데 파일이 없었다. `.gitignore`의 `.env*`가 예제까지 잡고 있었다 | **완료** — `.env.example` 생성 + `!.env.example` 예외 추가 |
| **P6-7** | `dev`→`main`이 fast-forward라 **부분 릴리스가 불가능** | `main` 이력에 머지 커밋 0건(완전 선형) | 지금 규모에선 문제 아님. 다만 "핫픽스만 먼저" 상황에 대비해 **핫픽스는 `main`에서 분기 → 양쪽 반영** 규칙만 정해둘 것 |

> **P6-1이 가장 깊은 결함이다.** 코드 배포(git push → Vercel)와 스키마 변경(수동 Management API)이
> **서로를 모르는 두 파이프라인**이다. 지금 `011_fix_autoclose_overflow.sql`이 그 실증이다 —
> `main`에 머지되면 코드는 정상 배포되지만 DB 함수는 그대로라 **버그가 남은 채 아무도 모른다.**

---

## 권고 착수 순서

1. **P0-3**(Preview DB 확인) — 가장 싸고, 틀리면 피해가 가장 크다
2. **P0-1**(dev→main 머지) — 이미 고친 치명 결함들이 운영에 안 가 있다
3. **P0-2**(011 적용) — 코드 배포로는 안 고쳐지는 유일한 항목
4. **P2-1 · P2-2**(fail-open + 성별 필수) — 한 줄짜리 수정인데 추천 품질에 직결
5. **P1-1 · P1-2**(연락처 스냅샷·차단 시 정보 숨김) — 실사용자 늘기 전에
6. **P2-12 · P2-14**(중요도 가중치 + `intent`) — 설문 v2의 핵심 2개
7. **P3-1 · P3-2**(백업·에러 모니터링) — 장애를 인지할 수단
